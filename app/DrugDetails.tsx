import { View, Text } from "react-native";
import React from "react";
import DrugDetails from "@/components/Core/DrugDetails";

const Details = () => {
  return <DrugDetails />;
};

export default Details;
